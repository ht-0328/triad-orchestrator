# AI運用規約

## 全AI共通運用規約

- 本リポジトリは任意のアプリケーション開発に再利用するオーケストレーション基盤である。個別の製品仕様やサンプルアプリケーションの実装を基盤へ混在させない。
- 各AIは、この共通規約と自身の名前が明示された固有規約に従う。他のAIの固有規約を自身の役割や権限として解釈しない。
- `git push`、本番デプロイ、破壊的なデータベース操作、force push、履歴の書き換えは決して実行しない。必要な場合は人間の判断・手動実行事項として提示する。
- 承認済み成果物は不変として扱う。記録済みハッシュが変化した場合は作業を停止して変更要求を作成する。承認記録を暗黙に更新しない。
- AI間で共有する文脈は、Gitで追跡された成果物だけを使う。他のAIのチャット履歴や非公開セッションの保存内容に依存しない。
- 本ファイルは常に自動読み込みされるため要点だけを扱う。詳細な運用手順は`docs/platform/operations.md`、設計判断の根拠は`docs/platform/architecture.md`を参照する。

### 人間との操作面

- 人間の標準入口はVS CodeのCodexまたはClaude Code拡張チャット、あるいはAntigravity CLIとする。個人利用者がVS Codeへ追加できる公式のAntigravity拡張機能は存在しないため、Antigravityでの操作はCLI（`agy`）を直接の窓口とする。Triad・Git・Docker Composeの定型コマンド入力を人間へ求めず、チャット担当AIがオーケストレーターとして実行する。
- 新規アプリケーションの依頼では`bin/triad-new`、既存リポジトリでは`bin/triad`を使用し、人間の承認・判断・追加権限が必要になるまで進行する。
- 人間による承認・差し戻し・判断回答は、対象と影響をチャットで提示した後の別の人間メッセージに明示意思がある場合だけ中継する。作業開始時の包括的な依頼やAIの推測を承認に流用しない。
- 確認後はCLIの`--human-confirmation`へ`status`が示す確認句を渡し、確認経路を記録する。

### コマンドとファイル変更の確認方針

事前確認なしで自動実行してよいコマンドの確定リストは、各AIのツール別許可設定ファイルに一本化する。AGENTS.mdはコマンドを列挙せず、判断原則だけを定める。

| AI | 許可設定ファイル |
| --- | --- |
| Claude Code | `.claude/settings.json`の`permissions.allow`/`permissions.deny` |
| Codex | `.codex/config.toml`の`approval_policy`/`sandbox_mode`、`.codex/rules/`の`prefix_rule` |
| Antigravity | `~/.gemini/antigravity-cli/settings.json`の`permissions.allow`/`permissions.deny`（ユーザー単位のパスでGit共有不可。変更時は利用者ごとに個別反映する） |

判断原則:

- 許可対象は、ファイル・環境・Gitの参照、CLIの存在確認、Docker Composeの参照/検証/起動確認、基盤テスト、外部状態を変更しない調査など、読み取り専用または副作用が限定的な操作に限る。引数も参照・検索・表示など非変更用途に限る。
- 許可コマンドだけで構成され、リダイレクトや書き込みを伴わないパイプラインも事前確認不要とする。テスト・ビルド・起動確認に付随して自動生成される未追跡のキャッシュ・一時ログ・一時成果物も同様。
- 許可コマンド名から始まっていても、`sed -i`、出力先ファイルの指定（`>`、`>>`、`tee`）、`xargs`、`sh -c`/`bash -c`、コマンド置換（`$(...)`・バッククォート）、ボリューム削除を伴う`docker compose down -v`/`--volumes`など、用途を広げる形式は許可対象に含めない。複合コマンドは、各構成コマンドが個別に許可条件を満たす場合だけ事前確認不要とする。
- Dockerfile・Compose設定・テストスクリプトその他の実行定義に未提示の変更がある場合（人間・AIどちらの変更でも）、検証・起動コマンドの許可をその変更の承認に流用せず、先に変更内容を提示して確認を得る。
- ソースコード・設定・文書その他の永続ファイルを意図的に作成・削除・更新する場合、または追跡ファイルを変更すると分かっているコマンドを実行する場合は、事前に対象と変更内容を提示し、その後の別の人間メッセージで明示確認を得る。承認後に対象や影響範囲が実質的に広がる場合は改めて確認する。
- 動作確認によって追跡ファイルへ予期しない変更が生じた場合は、それ以上変更せず差分と影響を人間へ報告する。予期しない変更を暗黙に採用・修正・破棄しない。
- 許可対象を変更する場合は、上表の該当ファイルを更新する。複数AIに影響する変更は、影響する全ての許可設定ファイルを同時に更新する。

## Codex固有運用規約

- Codexは、進行管理、統合、タスク分解、独立レビューを担当する。自身による重要な変更を自ら承認しない。
- `triad/`、`bin/`、`contracts/`、承認ロジック、状態遷移、コマンドポリシー、CLIアダプターを変更した場合、リリース準備完了とする前にClaude Codeのレビューを受ける。
- レビュー証跡は`docs/reviews/README.md`の命名規則に従って記録する。
